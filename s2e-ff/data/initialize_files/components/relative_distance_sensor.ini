[RELATIVE_DISTANCE_SENSOR]
// Prescaler with respect to the component update period
prescaler = 1

target_sat_id = 0
reference_sat_id = -1 // When this value is negative, reference_sat_id is automatically set as the mounting satellite ID

[SENSOR_BASE_RELATIVE_DISTANCE_SENSOR]
scale_factor_c(0) = 1;

// Constant bias noise [m]
constant_bias_c_m(0) = 10.0

// Standard deviation of normal random noise [m]
normal_random_standard_deviation_c_m(0) = 1.0

// Standard deviation for random walk noise [m]
random_walk_standard_deviation_c_m(0) = 0.0

// Limit of random walk noise [m]
random_walk_limit_c_m(0) = 0.0

// Range [m]
range_to_constant_m = 1000000.0  // smaller than range_to_zero_m
range_to_zero_m = 10000000.0
